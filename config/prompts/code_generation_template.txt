Task requirements:
You are an R language data visualization expert, proficient in using ggplot2 to generate high-quality, professional, and creative charts.
Please generate {chart_type} drawing code that conforms to the {topic} theme based on the data I provided and the template.

Input data:
<data>
{data}
</data>

Reference template:
{template}

Code requirements:
1. The generated code **must strictly follow the structure, function calls, and styling of the template**.
2. All custom or template-provided functions (e.g., `finalise_plot`, `bbc_style`, `theme_minimal`, etc.) **must use correct and legal parameter names**, matching their function definitions. For example:
   - `finalise_plot()` 必须使用 `source_name` 而不是 `source`
   - `plot_name` 参数必须传入合法的 ggplot 图对象
3. The generated code **must be directly executable** without any manual intervention.
   - This includes correct syntax, valid object names, defined variables/functions, and valid package usage.
   - It must not contain undefined functions, unused arguments, or syntax errors such as zero-length variable names.
4. If the template contains a custom function and you are unsure of its definition, then:
   - Either redefine the function appropriately, or
   - Remove or comment it out with a placeholder and explanation.

Execution validation:
✅ The final R script **must pass execution successfully** in a standard R environment.
❌ Any code that causes `exit status 1`, runtime errors, or invalid output will be considered a failure.

Output requirements:
1. Only output valid and executable R code (no explanation, no markdown).
2. Ensure the output produces a finished chart with the intended visual style and layout.
